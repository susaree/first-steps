Purpose: make you wake up before 8am and after 10pm if your dog barks, but do not do so in any other case.

public static boolean bark(boolean barking, int hourOfDay) {
        if(barking){
            if(hourOfDay >= 0 && hourOfDay < 8 || hourOfDay > 22 && hourOfDay <= 23 ) {
                return true;
            }

        }
      return false;
    }
